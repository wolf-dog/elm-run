{
  "name": "elm-runner",
  "version": "1.0.0",
  "license": "MIT",
  "dependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.2"
  },
  "scripts": {
    "build": "elm make src/Main.elm --output public/main.js --optimize",
    "format": "elm-format src/*.elm"
  }
}
